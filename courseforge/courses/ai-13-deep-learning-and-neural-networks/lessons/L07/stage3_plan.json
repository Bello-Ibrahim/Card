{
  "title": "Convolutional Neural Networks",
  "thumbnail": {
    "headline": "Small Filters, Big Results",
    "image": "Navy background, a small teal 3×3 grid sliding across a greyscale clothing image and lighting up edges, headline in teal Inter Bold.",
    "slide": {"layout": "title", "title": "Small Filters, Big Results", "body": "A 3×3 filter window sliding over a 28×28 image, leaving a bright feature map behind. Small tag: Deep Learning and Neural Networks · Lesson 7."}
  },
  "production_notes": [
    "[VERIFY] Fashion-MNIST licence and source (carried from L06): confirm before the dataset is used in the recording.",
    "Parameter counts on screen must match content.md: 50186 for the CNN and 235146 for the fully connected network.",
    "Accuracy: content.md gives no numbers. The voiceover says 'you should see something like the CNN reaching a higher validation accuracy'. Show the real measured values; do not add numbers in captions.",
    "Hook figures (about 150 million weights for a 224×224×3 → 1,000 fully connected layer; under 2,000 for 64 filters of 3×3×3 plus biases) are from content.md.",
    "On CPU, train on a 10,000-image subset so each epoch takes a few minutes; speed up the training segment in the edit.",
    "Rafael and the Recife e-commerce company are hypothetical; stock footage must not show a real company name or logo."
  ],
  "sections": {
    "Hook": [
      {"visual_type": "presenter_full",
       "voiceover": "A fully connected layer that reads a small colour photo and outputs one thousand features needs about one hundred and fifty million weights. A convolutional layer that finds sixty-four patterns in the same photo needs under two thousand. How?",
       "on_screen_text": "150 million weights or 2,000?"}
    ],
    "Explain": [
      {"visual_type": "slide",
       "voiceover": "A fully connected network flattens the image into one long row. It ignores the fact that nearby pixels belong together. And a pattern it learns in the top-left corner must be learned again for every other position.",
       "slide": {"layout": "diagram", "title": "The problem with flattening", "body": "A 28×28 image unrolled into one long row of 784 numbers; two neighbouring pixels end up far apart. The same pattern in two corners = two separate sets of weights."}},
      {"visual_type": "slide",
       "voiceover": "A convolution fixes both problems. A filter is a small grid of weights, for example three by three. It slides across the image, and at each position it computes a weighted sum of the pixels under it. The result is a feature map that is high wherever the pattern appears.",
       "slide": {"layout": "diagram", "title": "A filter slides across the image", "body": "3×3 filter moves step by step over the image; each position gives one number in the output feature map, bright where the pattern (an edge) appears."}},
      {"visual_type": "slide",
       "voiceover": "Two ideas make this efficient. Local connections, because each output looks only at a small patch. And weight sharing, because the same filter is used everywhere, so a pattern learned once is found anywhere.",
       "slide": {"layout": "bullets", "title": "Why it is efficient", "body": "Local connections: each output sees a small patch · Weight sharing: one filter, every position · nn.Conv2d(in, out, kernel_size, padding): out filters of in × k × k weights + bias"}},
      {"visual_type": "slide",
       "voiceover": "Pooling keeps the largest value in each two by two block, which halves the height and width. A small CNN repeats convolution, ReLU and pooling a few times. The image gets smaller while the channels grow. Early layers learn edges, and later layers learn shapes.",
       "slide": {"layout": "diagram", "title": "Conv → ReLU → Pool", "body": "1×28×28 → [conv, ReLU, pool] → 32×14×14 → [conv, ReLU, pool] → 64×7×7 → Flatten → Linear → 10 classes. Labels under blocks: edges → shapes."}},
      {"visual_type": "stock",
       "voiceover": "Picture a small window that you move across a large map, looking for one symbol, such as a bridge. You use the same window everywhere, so you learn once what a bridge looks like. Then a second person scans your list of bridges, rivers and roads, and finds bigger patterns, like a town.",
       "stock_query": "person studying large paper map with magnifier",
       "on_screen_text": "One window, used everywhere"}
    ],
    "Demonstrate": [
      {"visual_type": "stock",
       "voiceover": "Rafael is an engineer at an e-commerce company in Recife, Brazil. He compares a CNN with the fully connected network from lesson three, on Fashion-MNIST, using the transforms from the last lesson.",
       "stock_query": "online shop warehouse packing clothes",
       "on_screen_text": "Rafael · e-commerce · Recife"},
      {"visual_type": "screen",
       "voiceover": "In Colab, he defines the CNN. Two blocks of convolution, ReLU and pooling, with thirty-two and then sixty-four filters. Then flatten, and one linear layer to ten classes. He also defines the fully connected network for comparison.",
       "screen_steps": ["Run the model cell from content.md: cnn = nn.Sequential(nn.Conv2d(1, 32, 3, padding=1), nn.ReLU(), nn.MaxPool2d(2), nn.Conv2d(32, 64, 3, padding=1), nn.ReLU(), nn.MaxPool2d(2), nn.Flatten(), nn.Linear(64 * 7 * 7, 10))", "Same cell: mlp = nn.Sequential(nn.Flatten(), nn.Linear(784, 256), nn.ReLU(), nn.Linear(256, 128), nn.ReLU(), nn.Linear(128, 10))"]},
      {"visual_type": "screen",
       "voiceover": "He counts the parameters. Fifty thousand, one hundred and eighty-six for the CNN, and two hundred and thirty-five thousand, one hundred and forty-six for the other network. The CNN has about one fifth of the parameters.",
       "screen_steps": ["Run: count = lambda m: sum(p.numel() for p in m.parameters()); print(count(cnn), count(mlp))", "Output: 50186 235146"],
       "on_screen_text": "50,186 vs 235,146 parameters"},
      {"visual_type": "screen",
       "voiceover": "Next, the most useful debugging tool in this lesson. He passes a dummy batch through the CNN, one layer at a time, and prints the shape after each. He can see the image shrink and the channels grow, and the exact size the linear layer needs.",
       "screen_steps": ["Run: x = torch.randn(8, 1, 28, 28); for layer in cnn: x = layer(x); print(type(layer).__name__, tuple(x.shape))", "Point to (8, 32, 14, 14), then (8, 64, 7, 7), then Flatten (8, 3136), then Linear (8, 10)"],
       "on_screen_text": "Print shapes with a dummy batch"},
      {"visual_type": "screen",
       "voiceover": "He trains both models for five epochs with the loop from lesson five, the same optimiser and the same learning rate. You should see something like the CNN reaching a higher validation accuracy. Rafael reports his own measured numbers rather than assuming a result.",
       "screen_steps": ["Run the L05 training loop for cnn, 5 epochs; then for mlp with the same optimiser and lr (on CPU: 10,000-image subset)", "Show the final validation accuracy of each model side by side (expected output: CNN higher)"],
       "on_screen_text": "Same loop, same settings, fair test"},
      {"visual_type": "text",
       "voiceover": "A common mistake is calculating the linear layer's input size by hand, and getting it wrong after changing padding or pooling. The error message shows the real size. Print the shapes, or use a lazy linear layer, which works out its size on the first pass. And greyscale images still need a channel dimension of one.",
       "on_screen_text": "Let the shapes tell you the size"}
    ],
    "Recap": [
      {"visual_type": "slide",
       "voiceover": "Let's recap. First, a convolution slides a small shared filter across the image, so it finds a pattern anywhere with very few weights. Second, stacks of convolution, ReLU and pooling shrink the image and grow the channels, moving from edges to shapes to objects. Third, print shapes with a dummy batch to size the final layer, and compare models on parameters as well as accuracy.",
       "slide": {"layout": "bullets", "title": "Key takeaways", "body": "1. Shared filters find patterns anywhere with few weights. 2. Conv → ReLU → pool: smaller image, more channels. 3. Print shapes; compare parameters and accuracy."}}
    ],
    "CTA": [
      {"visual_type": "presenter_full",
       "voiceover": "Now it is your turn. In the exercise below this video, you will train a small CNN on Fashion-MNIST and compare its accuracy and parameter count with a fully connected network. It takes about forty minutes. In the next lesson, we learn to read learning curves, so you know what to change next. See you there.",
       "on_screen_text": "Next: Reading Learning Curves"}
    ]
  }
}
