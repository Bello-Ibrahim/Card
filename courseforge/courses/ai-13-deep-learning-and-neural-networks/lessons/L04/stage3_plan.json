{
  "title": "How Networks Learn: Loss and Backpropagation",
  "thumbnail": {
    "headline": "Walking Downhill in Fog",
    "image": "Navy background, a teal loss curve shaped like a valley with small footsteps walking down it through soft fog, headline in teal Inter Bold.",
    "slide": {"layout": "title", "title": "Walking Downhill in Fog", "body": "A U-shaped loss curve with footprints stepping down towards the lowest point; light fog over the top. Small tag: Deep Learning and Neural Networks · Lesson 4."}
  },
  "production_notes": [
    "Review flags: none. Run the snippet before recording and confirm the printed values: w.grad = tensor(-6.) and w after 10 steps = 2.678.",
    "For the lr = 1.1 comparison, change only the lr value in the same cell and re-run; the plotted loss must grow. Record both plots.",
    "Bilal and the Lahore logistics company are hypothetical; stock footage must not show a real company name or logo."
  ],
  "sections": {
    "Hook": [
      {"visual_type": "presenter_full",
       "voiceover": "The network from the last lesson has two hundred and thirty-five thousand parameters, all set to random values. Nobody will tune them by hand. So how does each one know which way to move? The answer is one line of code.",
       "on_screen_text": "How does each weight know?"}
    ],
    "Explain": [
      {"visual_type": "slide",
       "voiceover": "Training needs three parts. The first is a loss function. It gives one number that says how wrong the model is on a batch.",
       "slide": {"layout": "title", "title": "How Networks Learn: Loss and Backpropagation", "body": "Three parts: loss · gradients · gradient descent"}},
      {"visual_type": "slide",
       "voiceover": "For classes, use cross-entropy loss. It takes raw logits and whole-number class labels. For numbers, use mean squared error, with predictions and targets of the same shape. For yes or no labels, or several labels at once, there is a binary version that takes one logit per label.",
       "slide": {"layout": "bullets", "title": "1. Choose a loss", "body": "nn.CrossEntropyLoss: logits (N, C) + integer labels (N,) · nn.MSELoss: predictions and targets, same shape · nn.BCEWithLogitsLoss: binary or multi-label, one logit per label"}},
      {"visual_type": "slide",
       "voiceover": "The second part is gradients. A quick recap from the maths course. The gradient tells you how much the loss changes, and in which direction, if you move one parameter a little. Backpropagation is the chain rule, applied layer by layer from the loss back to the inputs.",
       "slide": {"layout": "diagram", "title": "2. Gradients by backpropagation", "body": "Forward arrows: input → layer 1 → layer 2 → loss. Backward arrows in teal from loss → layer 2 → layer 1, labelled 'chain rule'."}},
      {"visual_type": "slide",
       "voiceover": "You never write it yourself. PyTorch's autograd records every operation on tensors that need gradients. Then one call to backward fills in the gradient of every parameter.",
       "slide": {"layout": "code", "title": "Autograd does it for you", "body": "w = torch.tensor(0.0, requires_grad=True)\nloss.backward()   # fills w.grad"}},
      {"visual_type": "slide",
       "voiceover": "The third part is gradient descent. Each parameter moves a small step against its gradient. The learning rate sets the step size. Too small, and training is slow. Too large, and the loss jumps around or grows.",
       "slide": {"layout": "code", "title": "3. Gradient descent", "body": "w = w - lr * w.grad\nlr too small → slow · lr too large → loss jumps or grows"}},
      {"visual_type": "stock",
       "voiceover": "Picture walking downhill in thick fog. You cannot see the valley, but you can feel the slope under your feet. At each step, you move a little in the steepest downhill direction. Very long steps can take you past the valley and up the other side.",
       "stock_query": "hiker walking down foggy hillside",
       "on_screen_text": "Feel the slope, take a step"},
      {"visual_type": "slide",
       "voiceover": "Two details matter in code. Gradients add up with every backward call, so you reset them to zero before the next step. And the update itself must not be recorded, so you do it inside a no-grad block.",
       "slide": {"layout": "bullets", "title": "Two details in code", "body": "Gradients accumulate → reset with .grad.zero_() every step · Do the update inside torch.no_grad()"}}
    ],
    "Demonstrate": [
      {"visual_type": "stock",
       "voiceover": "Bilal is a data scientist at a logistics company in Lahore, Pakistan. Before he trusts optimisers, he tests gradient descent on a function he can check by hand. W minus three, squared. The lowest point is at three, and at zero the gradient should be minus six.",
       "stock_query": "data scientist working at laptop office",
       "on_screen_text": "Bilal · logistics · Lahore"},
      {"visual_type": "screen",
       "voiceover": "In Colab, he creates w at zero and asks PyTorch to track its gradient. He computes the loss, calls backward, and prints the gradient. Minus six, exactly as the hand calculation said.",
       "screen_steps": ["Run: w = torch.tensor(0.0, requires_grad=True); loss = (w - 3) ** 2; loss.backward(); print(w.grad)", "Output: tensor(-6.)"],
       "on_screen_text": "Gradient at zero: minus six"},
      {"visual_type": "screen",
       "voiceover": "Now the loop. For ten steps, he computes the loss, calls backward, moves w against the gradient inside a no-grad block, and resets the gradient to zero. The learning rate is zero point one.",
       "screen_steps": ["Run the loop cell: lr, history = 0.1, []; w.grad.zero_(); for step in range(10): loss = (w - 3) ** 2; loss.backward(); with torch.no_grad(): w -= lr * w.grad; w.grad.zero_(); history.append(loss.item())", "Highlight the with torch.no_grad() block and w.grad.zero_()"]},
      {"visual_type": "screen",
       "voiceover": "After ten steps, w is about two point six eight, close to three. The plot shows the loss falling quickly at first, and then more slowly. Each step moves w twenty percent of the remaining distance.",
       "screen_steps": ["Run: print(round(w.item(), 3)) → 2.678", "Run: plt.plot(history); plt.xlabel(\"step\"); plt.ylabel(\"loss\"); plt.show()", "Point to the curve falling fast, then flattening"],
       "on_screen_text": "Fast at first, then slower"},
      {"visual_type": "screen",
       "voiceover": "Then Bilal changes the learning rate to one point one. Now each step overshoots. W moves further from three every time, and the loss grows. You will see this same pattern in real learning curves in lesson eight.",
       "screen_steps": ["Reset w to 0.0, change lr to 1.1 in the loop cell and re-run", "Re-run the plot cell; point to the loss growing each step"],
       "on_screen_text": "Learning rate too high: loss grows"},
      {"visual_type": "slide",
       "voiceover": "In real training, an optimiser does the update and the reset for every parameter at once. Adam, which adapts the step size for each parameter, is a common default.",
       "slide": {"layout": "code", "title": "In real training", "body": "opt = torch.optim.SGD(model.parameters(), lr=0.1)\nopt.zero_grad()  # reset\nopt.step()       # update\nAdam: torch.optim.Adam, a common default"}},
      {"visual_type": "text",
       "voiceover": "The most common mistake is forgetting to zero the gradients. They keep adding up, so the steps grow larger and larger. The loss may fall, then jump or become not a number. It looks like a learning-rate problem, but it is not.",
       "on_screen_text": "Zero the gradients every step"}
    ],
    "Recap": [
      {"visual_type": "slide",
       "voiceover": "Let's recap. First, the loss is one number for how wrong the model is. Use cross-entropy with logits and integer labels for classes, and mean squared error for numbers. Second, backward uses autograd to compute every gradient by the chain rule. Gradients accumulate, so reset them every step. Third, gradient descent moves parameters against the gradient, and the learning rate sets the step size.",
       "slide": {"layout": "bullets", "title": "Key takeaways", "body": "1. Loss: cross-entropy for classes, MSE for numbers. 2. loss.backward() computes gradients; reset them every step. 3. Step against the gradient; lr too small is slow, too large diverges."}}
    ],
    "CTA": [
      {"visual_type": "presenter_full",
       "voiceover": "Now it is your turn. In the exercise below this video, you will pick a function with a known minimum, check its gradient with autograd, take ten manual steps, and plot the loss for a good and a bad learning rate. It takes about twenty-five minutes. In the next lesson, we put it all together in the training loop. See you there.",
       "on_screen_text": "Next: The Training Loop"}
    ]
  }
}
