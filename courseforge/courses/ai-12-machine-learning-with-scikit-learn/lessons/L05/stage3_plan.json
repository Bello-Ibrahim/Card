{
  "title": "Pipelines and ColumnTransformer",
  "thumbnail": {
    "headline": "One Object, One Fit",
    "image": "Navy background, an assembly line of three stations (impute, scale, encode) feeding a model box, headline in teal Inter Bold.",
    "slide": {
      "layout": "title",
      "title": "One Object, One Fit",
      "body": "Assembly line: raw table → sorting point splits numbers and categories → impute + scale / one-hot → join → model. Tag: Machine Learning with scikit-learn · Lesson 5."
    }
  },
  "production_notes": [
    "content.md lists this lesson as 6 minutes; the script is held to the brief's 5-minute word range (630 to 770 words), so it runs about five and a half minutes.",
    "[VERIFY] The voiceover does not describe the UCI Bank Marketing dataset (source, column names campaign and duration, target y, licence). Content.md says it comes from a Portuguese bank's phone campaign for term deposits and that duration is only known after the call; both claims stay here for review and are not spoken.",
    "[VERSION] set_output(transform=\"pandas\"), OneHotEncoder(sparse_output=...) and the pipeline diagram display in Colab depend on the installed scikit-learn version. Outputs were recorded with scikit-learn 1.9.1, pandas 3.0.6 and Python 3.11; re-run and update the spoken 0.884 if the output differs.",
    "Setup: scene 9 runs the shared setup cell (content.md Worked Example, first code block). This cell and the pipeline cell in scene 11 are reused in L06, L08, L09, L10, L14, L15, L16 and L17, so save the recording notebook as the course notebook.",
    "Scene 13: after the pipeline cell, run a new cell containing only pipe to show the Colab diagram, as content.md describes.",
    "Oluwaseun Adeyemi is fictional; the data is synthetic."
  ],
  "sections": {
    "Hook": [
      {
        "visual_type": "presenter_full",
        "voiceover": "In the last lesson, you scaled and encoded columns by hand, with separate objects and careful ordering. In a real project with fifteen columns and many experiments, one small slip lets test data leak into training. Today you will remove that risk.",
        "on_screen_text": "One small slip, one big leak"
      }
    ],
    "Explain": [
      {
        "visual_type": "slide",
        "voiceover": "A pipeline chains steps into one estimator. Every step except the last one is a transformer, and the last step is the model. When you fit the pipeline on training data, each step learns from the training data and passes its output to the next step.",
        "slide": {
          "layout": "diagram",
          "title": "A pipeline",
          "body": "Step 1 transformer → Step 2 transformer → Last step: model. Caption: pipe.fit(X_train, y_train) fits every step on training data."
        }
      },
      {
        "visual_type": "slide",
        "voiceover": "When you predict, each step only transforms. So fitting on the training data only happens automatically. You do not have to remember the order yourself.",
        "slide": {
          "layout": "comparison",
          "title": "fit vs predict in a pipeline",
          "body": "fit: every step learns from training rows. predict: every step only transforms, then the model predicts."
        }
      },
      {
        "visual_type": "slide",
        "voiceover": "A column transformer sends different columns to different steps. Numbers go to filling missing values and scaling. Categories go to one-hot encoding. Then the outputs are placed side by side again.",
        "slide": {
          "layout": "diagram",
          "title": "ColumnTransformer",
          "body": "Table splits: number columns → impute + scale; category columns → one-hot encode; both outputs joined side by side."
        }
      },
      {
        "visual_type": "slide",
        "voiceover": "This matters most for data leakage. Leakage means information that would not be available at prediction time reaches the model during training. It makes test scores look better than real performance. Pipelines stop one kind, preprocessing that learns from test rows. They also make cross-validation and tuning safe later in the course.",
        "slide": {
          "layout": "bullets",
          "title": "Data leakage",
          "body": "Information not available at prediction time reaches training · Test scores look too good · Pipelines stop preprocessing leakage · They make cross-validation (L10) and tuning (L14) safe"
        }
      },
      {
        "visual_type": "text",
        "voiceover": "But pipelines cannot stop the other kind of leakage: a feature that is recorded after the event. You must remove that yourself, as Larissa did in lesson one.",
        "on_screen_text": "After-the-event features: remove them yourself"
      },
      {
        "visual_type": "stock",
        "voiceover": "Think of a factory assembly line. Each station does one job in a fixed order, and a product cannot skip a station or enter halfway. The column transformer is where parts are sorted. Metal parts go to one station, plastic parts to another, and then they are joined again.",
        "stock_query": "factory assembly line stations products moving",
        "on_screen_text": "A fixed order, every time"
      }
    ],
    "Demonstrate": [
      {
        "visual_type": "stock",
        "voiceover": "Oluwaseun Adeyemi is a developer in Lagos. He wants to predict which bank customers will subscribe to a term deposit. To keep the demo repeatable, the course notebook uses synthetic bank marketing data, where about twelve percent of customers subscribe.",
        "stock_query": "young developer laptop office Lagos",
        "on_screen_text": "Oluwaseun · developer · Lagos"
      },
      {
        "visual_type": "screen",
        "voiceover": "First, we run the setup cell. It creates two thousand synthetic customers with age, balance, number of calls, job and contact type. It hides one hundred balances as missing values. Then it splits the data once, keeping a quarter for testing. We will reuse this cell for most of the course.",
        "screen_steps": [
          "Open a new Colab notebook and add a text cell: Setup cell, shared by L05 to L17.",
          "Type this exact code cell from content.md and run it with Shift+Enter:\nimport numpy as np, pandas as pd\nfrom sklearn.model_selection import train_test_split\n\nrng = np.random.default_rng(42)\nn = 2000\ndf = pd.DataFrame({\n    \"age\": rng.integers(18, 80, n),\n    \"balance\": rng.normal(1500, 800, n).round(),\n    \"calls\": rng.integers(1, 8, n),\n    \"job\": rng.choice([\"admin\", \"technician\", \"services\", \"retired\"], n),\n    \"contact\": rng.choice([\"cellular\", \"telephone\"], n),\n})\nscore = (-3 + 0.03 * (df[\"age\"] - 40) + 0.0006 * df[\"balance\"] - 0.3 * df[\"calls\"]\n         + 1.2 * (df[\"job\"] == \"retired\") + 0.8 * (df[\"contact\"] == \"cellular\"))\ndf[\"subscribed\"] = (rng.random(n) < 1 / (1 + np.exp(-score))).astype(int)\ndf.loc[rng.choice(n, 100, replace=False), \"balance\"] = np.nan\n\nX, y = df.drop(columns=\"subscribed\"), df[\"subscribed\"]\nX_train, X_test, y_train, y_test = train_test_split(\n    X, y, test_size=0.25, stratify=y, random_state=42)"
        ],
        "on_screen_text": "Setup cell: shared by L05 to L17"
      },
      {
        "visual_type": "screen",
        "voiceover": "Look at the last lines of the setup cell. The target is whether the customer subscribed, and the features are all the other columns. The split keeps the same share of subscribers in both sets, and uses a fixed random state, so your numbers will match mine.",
        "screen_steps": [
          "Highlight the line X, y = df.drop(columns=\"subscribed\"), df[\"subscribed\"].",
          "Highlight stratify=y and random_state=42 in the train_test_split call."
        ],
        "on_screen_text": "Target: subscribed · stratified split"
      },
      {
        "visual_type": "screen",
        "voiceover": "Now the pipeline cell. For numbers, a small pipeline fills missing values with the median, then scales. For categories, a one-hot encoder. The column transformer sends each list of columns to its own step, and logistic regression is the final model. Then one fit call, and we print the test accuracy.",
        "screen_steps": [
          "Type this exact code cell from content.md and run it with Shift+Enter:\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.compose import ColumnTransformer\nfrom sklearn.impute import SimpleImputer\nfrom sklearn.preprocessing import StandardScaler, OneHotEncoder\nfrom sklearn.linear_model import LogisticRegression\n\nnum_cols = [\"age\", \"balance\", \"calls\"]\ncat_cols = [\"job\", \"contact\"]\nnumeric = Pipeline([(\"impute\", SimpleImputer(strategy=\"median\")),\n                    (\"scale\", StandardScaler())])\nprep = ColumnTransformer([\n    (\"num\", numeric, num_cols),\n    (\"cat\", OneHotEncoder(handle_unknown=\"ignore\"), cat_cols),\n])\npipe = Pipeline([(\"prep\", prep), (\"model\", LogisticRegression(max_iter=1000))])\npipe.fit(X_train, y_train)\nprint(round(pipe.score(X_test, y_test), 3))"
        ],
        "on_screen_text": "Numbers and categories, each to its step"
      },
      {
        "visual_type": "screen",
        "voiceover": "The test accuracy is zero point eight eight four. Notice what happened. The one hundred missing balances were filled with the training median, inside the pipeline. And that single fit call ran every step, in the right order.",
        "screen_steps": [
          "Highlight the output 0.884.",
          "Highlight pipe.fit(X_train, y_train) and add a caption: one fit, every step."
        ],
        "on_screen_text": "Test accuracy: 0.884"
      },
      {
        "visual_type": "screen",
        "voiceover": "In Colab, you can display the pipeline itself, and you get a diagram of the steps. Keep that score of zero point eight eight four in mind. In lesson eight, you will see why it is less impressive than it looks.",
        "screen_steps": [
          "In a new cell, type pipe and run it.",
          "Click to expand the diagram: prep (num: impute, scale; cat: one-hot) and model.",
          "Point back to 0.884."
        ],
        "on_screen_text": "Remember 0.884 for lesson 8"
      },
      {
        "visual_type": "text",
        "voiceover": "A common mistake is to build a good pipeline, and then fill missing values or scale the full table first, to clean it. The pipeline is then fed data that already contains test information. Put every step that learns from data inside the pipeline. And check each column's meaning, so no feature is only known after the event.",
        "on_screen_text": "Every learning step goes inside"
      }
    ],
    "Recap": [
      {
        "visual_type": "slide",
        "voiceover": "Let's recap. First, a pipeline chains preprocessing and a model into one estimator, so one fit call learns every step from training data only. Second, a column transformer applies different steps to numbers and categories, and joins the results. Third, pipelines prevent preprocessing leakage, but you must still remove features that are only known after the event.",
        "slide": {
          "layout": "bullets",
          "title": "Key takeaways",
          "body": "1. Pipeline = preprocessing + model; one fit, training data only. 2. ColumnTransformer: different steps for numbers and categories. 3. Pipelines stop preprocessing leakage; you remove after-the-event features."
        }
      }
    ],
    "CTA": [
      {
        "visual_type": "presenter_full",
        "voiceover": "Now it is your turn. In the exercise below this video, you will build a pipeline that fills, scales, encodes and fits a model in one call, and display its diagram. Use public data only. It takes about thirty minutes. In the next lesson, Logistic Regression for Yes/No Questions, we look inside this model. See you there.",
        "on_screen_text": "Next: Logistic Regression"
      }
    ]
  }
}
